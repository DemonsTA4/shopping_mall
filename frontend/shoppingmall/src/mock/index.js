/* import { setupMockServer } from './mock-server'

// 只在开发环境且启用mock时启动Mock服务
const useMock = import.meta.env.DEV && (import.meta.env.VITE_USE_MOCK !== 'false')

if (useMock) {
  console.log('启动Mock服务')
  setupMockServer()
} else {
  console.log('使用真实后端服务')
} */